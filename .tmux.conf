unbind %
bind | split-window -h 

unbind '"'
bind - split-window -v

unbind Left
unbind Down
unbind Up
unbind Right

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
