# Profiler for performance analysis (recommended to leave as a note in daily use)
# zmodload zsh/zprof

# Access built-in file functions (like zcp, zln)
zmodload zsh/files

# Built-in and powerful calculator
# zmodload zsh/zcalc

# Advanced mathematical functions
zmodload zsh/mathfunc
